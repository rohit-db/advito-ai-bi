# CLAUDE.md

Agent guidance for this repo lives in **[`AGENTS.md`](AGENTS.md)** — read it first.
It covers what the app is, the doc map, the load-bearing invariants (tenant
isolation, the SP resolver seam, secret handling, fail-soft behavior), and dev
commands. The canonical deep-dive docs are under `docs/`.
